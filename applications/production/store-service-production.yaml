apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: store-service-production
  namespace: apps
  labels:
    app.kubernetes.io/name: store-service
    app.kubernetes.io/part-of: canary-demo
    environment: production
spec:
  project: default
  source:
    repoURL: https://github.com/rinsecrm/manifests-microservices.git
    targetRevision: HEAD
    path: kustomize/store-service/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - PrunePropagationPolicy=foreground
      - PruneLast=true
